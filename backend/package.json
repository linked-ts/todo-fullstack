{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["todo", "express", "typescript"],
  "author": "",
  "license": "ISC",
  "description": "Express TypeScript backend for TodoApp",
  "dependencies": {
    "@types/express": "^5.0.2",
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "@types/cors": "^2.8.17"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
